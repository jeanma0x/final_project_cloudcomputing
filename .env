# === App ===
APP_PORT=8000          # Puerto interno de la API FastAPI expuesto por uvicorn.
APP_ENV=development    # Entorno (dev/prod) por si luego cambiamos comportamientos.

# === MySQL (usadas por el contenedor db y por la API) ===
MYSQL_ROOT_PASSWORD=supersecure_root  # Password del súper-usuario de MySQL
MYSQL_DATABASE=users_db               # Base de datos a crear/inicializar
MYSQL_USER=app_user                   # Usuario de aplicación (no root)
MYSQL_PASSWORD=supersecure_app        # Password del usuario de app

# === CORS ===
CORS_ORIGINS=*        # En desarrollo permitimos todo. En prod: lista separada por comas.

# === JWT (para la siguiente fase: login/register) ===
JWT_SECRET=change_this_in_prod  # Cambia en producción; úsalo como secreto fuerte.
JWT_ALGORITHM=HS256             # Algoritmo de firma del token.
JWT_EXPIRES_MIN=60              # Expiración del token en minutos.
